## Prompt (short version): Add short descriptive comments above each `@testset` in `test/runtests.jl` so each test block is self-explanatory. (yasin)

Tool: GitHub Copilot (MAI-Code-1-Flash)

**What was asked:**  
Add explanatory comments to the test suite without changing behavior.

**What the LLM did:**  
- Inserted one-line comments above each test block in `test/runtests.jl`.
- Kept the comments concise and aligned with the purpose of each test group.

**Files changed:**  
- `test/runtests.jl`

## Prompt (short version): Add docstrings for `scripts/AddExampleFiles.jl`. (yasin)

Tool: GitHub Copilot (MAI-Code-1-Flash)

**What was asked:**  
Add clear docstrings to the helper functions in `scripts/AddExampleFiles.jl` without changing behavior.

**What the LLM did:**  
- Added docstrings for gzip/zip compression and decompression helpers.
- Documented the example model and dataset download workflow.

**Files changed:**  
- `scripts/AddExampleFiles.jl`

## Prompt (short version): Add docstrings for `scripts/PrepareToyDataset.jl`. (yasin)

Tool: GitHub Copilot (MAI-Code-1-Flash)

**What was asked:**  
Add descriptive docstrings to the dataset-generation helpers in `scripts/PrepareToyDataset.jl`.

**What the LLM did:**  
- Documented the dataset loading function.
- Added docstrings for the toy dataset generation workflow and its parameters.

**Files changed:**  
- `scripts/PrepareToyDataset.jl`

## Prompt (short version): Add docstrings for `scripts/ToyDemo.jl`. (yasin)

Tool: GitHub Copilot (MAI-Code-1-Flash)

**What was asked:**  
Add concise docstrings to the toy demo helpers in `scripts/ToyDemo.jl` without changing functionality.

**What the LLM did:**  
- Documented the word inspection helper.
- Added docstrings for automatic query-word selection and the main toy demo runner.

**Files changed:**  
- `scripts/ToyDemo.jl`

